# Web framework
django>=6.0,<7.0
python-dotenv>=1.0

# HTTP clients
httpx>=0.27
requests>=2.31

# LLM providers
openai>=1.40          # used for all OpenAI-compatible servers (Ollama, LM Studio, llama-server)
google-genai>=1.0     # Gemini
mistralai>=1.0        # Mistral OCR

# PDF / OCR
markitdown[pdf]>=0.1  # fast local text-layer extraction (first pass before OCR)
pymupdf>=1.24
pytesseract>=0.3.10   # requires the `tesseract` binary on PATH
pillow>=10.0
ocrmac>=1.0; sys_platform == "darwin"   # macOS Vision OCR

# Bibliography
pyzotero>=1.5
pybtex>=0.24
